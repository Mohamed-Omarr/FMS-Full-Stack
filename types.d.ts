type login = {
    email: string;
    password:string;
}

type register = {
    email: string;
    password:string;
    confirmPassword:string;
}

type user = {
    id: string;
    name: string;
    email: string;
}
